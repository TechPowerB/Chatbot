import { Partials } from "discord.js";

const { GuildMember, Message, Channel } = Partials;
const DiscordPartials = [GuildMember, Message, Channel];

export default DiscordPartials;

/**
 * Add partials here, if you wana use this bot base for something more
 * Else if you are just using this for normal chatting no need to edit something
 */
